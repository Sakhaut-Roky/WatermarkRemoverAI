# Application Configuration
PROJECT_NAME="WatermarkRemoverAI"
VERSION="0.1.0"
DEBUG=false

# Server Configuration
HOST="0.0.0.0"
PORT=8000
RELOAD=false

# Hardware / Acceleration Configuration (cuda, cpu, mps)
DEVICE="cpu"
DEFAULT_CONFIDENCE_THRESHOLD=0.5
